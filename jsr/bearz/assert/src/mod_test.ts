import { equal } from "./mod.ts";

// DO NOT REMOVE
// This line is used by automation to convert to vitest
const test = Deno.test;

test("assert::equal", () => {
    equal(true, true);
});


// +DENO
